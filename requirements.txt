# === Web 框架 ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
sse-starlette>=2.1.0

# === 数据库 ===
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.14.0
pgvector>=0.3.6
psycopg2-binary>=2.9.9

# === Redis ===
redis>=5.1.1

# === LLM / Agent 编排 ===
langchain>=0.3.14
langchain-openai>=0.3.3
langgraph>=0.2.55
langchain-community>=0.3.14
# Postgres checkpointer（跨进程会话持久化，可选）：安装后设置 CHECKPOINT_POSTGRES_URI 即启用，
# 未安装时 LangGraph 自动回退到内存 checkpointer（单进程内跨请求恢复会话）。
# langgraph-checkpoint-postgres>=2.0.0

# === Embedding / Reranker（本地模型）===
sentence-transformers>=3.3.1
torch>=2.4.0  # CPU 版即可

# === 文档解析 ===
pdfplumber>=0.11.4
python-docx>=1.1.2
beautifulsoup4>=4.12.3
lxml>=5.3.0
unstructured[all-docs]>=0.16.10
html2text>=2024.2.26

# === 检索 ===
rank-bm25>=0.2.2
jieba>=0.42.1

# === 配置管理 ===
pydantic>=2.10.2
pydantic-settings>=2.6.1

# === 鉴权 ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.1

# === 工具 ===
httpx>=0.27.2
python-dotenv>=1.0.1
aiofiles>=24.1.0

# === 评测 ===
ragas>=0.2.0
datasets>=3.0.1

# === 测试 ===
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx  # TestClient

# === 导出报告 ===
markdown>=3.7
# PDF 导出已改用 Chrome headless（--print-to-pdf），不再依赖 weasyprint

# === Docker / 部署 ===
gunicorn>=23.0.0
